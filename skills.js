// function to covert Celsius
// to Fahrenheit
function cToF(celsius) {
  return (celsius * 9/5) + 32;
}

// Driver code
const celsius = 32;
console.log(celsius + " degree celsius is equal to " + cToF(celsius) + " degree Fahrenheit");